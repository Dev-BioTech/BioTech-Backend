# Commercial Service

## Dependencies
This service does not explicitly call other microservices via HTTP in the current configuration.

## Configuration
This service listens on port `5238` by default in local development.
