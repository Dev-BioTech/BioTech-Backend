# Feeding Service

## Dependencies
This service does not explicitly call other microservices via HTTP in the current configuration.

## Consumers
- **AIService**: Consumes feeding data for analysis.

## Configuration
This service listens on port `5102` by default in local development.
