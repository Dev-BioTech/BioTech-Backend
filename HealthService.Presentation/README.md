# Health Service

## Dependencies
This service does not explicitly call other microservices via HTTP in the current configuration.

## Consumers
- **AIService**: Consumes health records for analysis.

## Configuration
This service listens on port `5158` by default in local development.
